// import {Router} from "express";
// import {createReader, deleteReader, getAllReaders, updateReader} from "../controller/reader.controller";
// import {authenticateToken} from "../middlewares/authentication";
//
// const readerRoutes = Router();
// readerRoutes.use(authenticateToken);
//
// readerRoutes.get('/', getAllReaders);
// readerRoutes.post('/', createReader);
// readerRoutes.put('/:id', updateReader);
// readerRoutes.delete('/:id', deleteReader);
//
// export default readerRoutes;